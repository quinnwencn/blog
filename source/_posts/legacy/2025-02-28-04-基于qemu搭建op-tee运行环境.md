---
title: "04 基于qemu搭建op-tee运行环境"
date: 2025-02-28 00:00:00
tags:
  - "TrustZone"
  - "TEE"
  - "ARMv8"
  - "QEMU"
  - "op-tee"
categories:
  - "Embedded Linux"
---

#111 介绍了如何在QEMU上模拟SoC启动ATF，以及分析了从BootROM到ATF，最后启动u-boot的过程。TEE的启动在 #111 中并没有实现，因为TEE OS的启动需要涉及ATF对SoC的内存划分，以及启动时序等。不过，OP-TEE有基于QEMU的ARMv8的全套模拟方案，可以实现BL1、BL2、BL31、BL32、BL33和Linux runtime，[OP-TEE QEMU v8](https://optee.readthedocs.io/en/latest/building/devices/qemu.html#qemu-v8).

## 克隆代码
1. 工具安装
OP-TEE使用的是git-repo管理仓库，在下载代码之前，需要先安装repo工具：
```bash
$ git clone -b v2.12.2 http://mirrors.ustc.edu.cn/aosp/git-repo.git
$ chmod u+x git-repo/repo
$ mkdir -p ~/bin
$ cp git-repo/repo ~/bin
$ echo "export PATH=~/bin:$PATH" >> ~/.zshrc  # or ~/.bashrc
$ source ~/.zshrc
```
2. 依赖安装
OP-TEE的编译依赖于一系列的工具，[OP-TEE文档](https://optee.readthedocs.io/en/latest/building/prerequisites.html)列出了这些以来，并且给出了基于Docker的Dockerfile，我们可以根据Dockerfile安装对应依赖（可以选择在Docker里仿真，也可以在host，本文在host进行）
```bash
$ sudo apt update -y
$ sudo apt upgrade -y

$ sudo apt install -y  \
    adb \
    acpica-tools \
    autoconf \
    automake \
    bc \
    bison \
    build-essential \
    ccache \
    cpio \
    cscope \
    curl \
    device-tree-compiler \
    e2tools \
    expect \
    fastboot \
    flex \
    ftp-upload \
    gdisk \
    git \
    libattr1-dev \
    libcap-ng-dev \
    libfdt-dev \
    libftdi-dev \
    libglib2.0-dev \
    libgmp3-dev \
    libhidapi-dev \
    libmpc-dev \
    libncurses5-dev \
    libpixman-1-dev \
    libslirp-dev \
    libssl-dev \
    libtool \
    libusb-1.0-0-dev \
    make \
    mtools \
    netcat \
    ninja-build \
    python3-cryptography \
    python3-pip \
    python3-pyelftools \
    python3-serial \
    python-is-python3 \
    rsync \
    swig \
    unzip \
    uuid-dev \
    wget \
    xdg-utils \
    xsltproc \
    xterm \
    xz-utils \
    zlib1g-dev
```

3. 下载OP-TEE QEMU仿真环境代码
```bash
$ mkdir optee
$ cd optee
$ repo init -u https://github.com/OP-TEE/manifest.git -m qemu_v8.xml
$ repo sync -j8
```
下载后的仓库如下所示：
```bash
$ tree -L 1 .
.
├── build                                 # 解决不同平台之间的Makefile和kconfig
├── buildroot                          # buildroot编译系统
├── hafnium                           # Hafnium 是一个开源的可信执行环境（TEE）的 hypervisor，通常用于 Arm 平台
├── linux                                 # Linux kernel源码
├── linux-arm-ffa-user           # 与 Arm Firmware Framework for Armv8-A（FF-A）相关的 Linux 用户空间实现 (猜测)
├── mbedtls                           # 轻量级的ssl/tls加密组件
├── ms-tpm-20-ref                # 微软的tpm组件
├── optee_client                    # op-tee的REE侧源码
├── optee_examples              # op-tee的TA/CA示例代码
├── optee_ftpm                     # 与 OP-TEE 的 fTPM（Firmware TPM）实现相关
├── optee_os                         # op-tee的TEE侧源码
├── optee_rust                      # 基于rust编写的TEE TA/CA框架和例子
├── optee_test                      # OP-TEE的测试源码
├── out                                  # 编译输出目录，主要存放用于烧录的image
├── out-br                             # buildroot编译输出目录
├── qemu                              # qemu源码
├── SCP-firmware                 # SCP（System Control Processor）固件，用于管理 Arm 系统的低级硬件功能
├── toolchains                       # 编译所需工具链
├── trusted-firmware-a         # atf源码   
├── trusted-services              # 与 OP-TEE 的可信服务实现相关
├── u-boot                            # u-boot源码
└── xen                                 # Xen 是一个开源 hypervisor，用于虚拟化环境
```
组件图：
![Image](https://github.com/user-attachments/assets/96b32d65-9c92-4e38-b276-0002fb2ca473)
来源：https://www.trustedfirmware.org/

## 编译和运行
OP-TEE仿真平台提供了一键式编译仓库build，它可以自动下载对应的交叉编译器，我们的编译和运行也都是在这个仓库里完成：
1. 编译
```bash
make -j8 toolchains # 下载和编译交叉编译器
make -j8 # 编译
···

2. 运行
运行就是基于QEMU，运行前面编译好的固件，因此我们使用qemu_v8的配置文件来运行：
```bash
make -f qemu_v8.mk run-only
```
执行运行后，就可以看到三个terminal窗口：
![Image](https://github.com/user-attachments/assets/2866d85b-c757-432d-8fce-7fadba8484d1)

* make -f qemu_v8.mk run-only：原始命令行terminal，启动了一个gdb调试
* Normal World：REE侧，运行这buildroot编译出来的runtime
* Secure World： TEE侧安全世界

在gdb侧输入c，按回车键，可以触发Normal World和Secure World启动：
![Image](https://github.com/user-attachments/assets/a2731f32-53b4-4a52-a1b0-b63f192e428e)

Normal World:
![Image](https://github.com/user-attachments/assets/64570e1e-7d6f-4f4d-ad44-d82838305f99)

Secure World:
![Image](https://github.com/user-attachments/assets/0183ad53-f9ac-40c1-ac1c-1c1ab2ec39a9)

我们可以在REE侧运行OP-TEE的例子，触发一次CA（Client Application）和TA（Trusted Application）的执行和交互：
REE:
![Image](https://github.com/user-attachments/assets/8fb3b372-5fda-4075-b766-70de0814c3a7)

TEE:
![Image](https://github.com/user-attachments/assets/079c096d-3572-4327-92fb-95611f4105dd)

[source issue](https://github.com/quinnwencn/blog/issues/112)
